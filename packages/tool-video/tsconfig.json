{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Reference package, pending rewrite against DESIGN.md. Unused symbols here are
    // the known-missing wiring (e.g. handleAddAssetToTimeline is never called, the
    // audio element ref is never used) rather than dead weight — deleting them would
    // delete the evidence. Restore these checks when the package is rewritten.
    "noUnusedLocals": false,
    "noUnusedParameters": false
  },
  "include": ["src/**/*"]
}
